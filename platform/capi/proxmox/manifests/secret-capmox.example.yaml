apiVersion: v1
kind: Secret
metadata:
  name: proxmox-credentials
  namespace: capi-system
  annotations:
    # NOTE: Check your provider docs; some variants read well-known key names or allow referencing Secret via env
    # This is an example only. Do not commit real credentials.
    infraflux.example: proxmox
type: Opaque
stringData:
  # Replace values below; keep them secret. Consider using External Secrets Operator instead of committing files.
  PROXMOX_URL: https://proxmox.example.com:8006
  PROXMOX_TOKEN_ID: root@pam!capmox
  PROXMOX_TOKEN_SECRET: "REDACTED"
