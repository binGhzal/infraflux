apiVersion: cluster.x-k8s.io/v1beta1
kind: Cluster
metadata:
  name: staging
  namespace: default
spec:
  topology:
    class: talos-proxmox
    version: v1.31.0
    controlPlane:
      replicas: 1
    workers:
      machineDeployments:
        - class: default-worker
          name: md-0
          replicas: 1
