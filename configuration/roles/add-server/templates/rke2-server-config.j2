write-kubeconfig-mode: "0644"
token: {{ token }}
server: https://{{ vip }}:9345
tls-san:
  - {{ vip }}
{% for host in groups['servers'] %}
  - {{ hostvars[host]['ansible_host'] }}
{% endfor %}
node-label:
  - server=true