---
# Apply Kubernetes manifests task file
# Note: This role is currently placeholder for future manifest application
# Cilium and other manifests are now deployed via GitOps/FluxCD

- name: Information about manifest deployment
  ansible.builtin.debug:
    msg: |
      Kubernetes manifests are now deployed via GitOps using FluxCD.
      This role is maintained for compatibility but manifests are applied
      automatically by the FluxCD controller after cluster deployment.
